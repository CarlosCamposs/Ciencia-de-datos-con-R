tinytex::install_tinytex()
library(nycflights13)
library(tidyverse)
library(knitr)
library(moderndive)
library(scales)
library(zoo)
##########
# Cargamos los datos
setwd("C:/Users/Carlos Daniel/Desktop/GitHub/Ciencia-de-datos-con-R/datasets")
weather %>% summarise(mean(temp),sd(temp))
library(nycflights13)
weather %>% summarise(mean(temp),sd(temp))
weather %>% summarize(mean(temp),sd(temp))
weather %>% summarize(media=mean(temp,na.rm=T),desviacion=sd(temp,na.rm=T))
weather %>%
summarize(media=mean(temp,na.rm=T),
desviacion=sd(temp,na.rm=T))
resumen_temp <- weather %>%
summarize(media=mean(temp,na.rm=T),
desviacion=sd(temp,na.rm=T))
resumen_temp
# Para no prender el na.rm=T pudimos haber realizado esto
weather %>%
filter(temp=!NA) %>%
summarize(mean(temp))
# ------------------------------------------------------------------------------
weather %>%
filter(origin=="JFK") %>%
summarize(mean(temp))
weather %>%
filter(temp %in% c(NA))
tabla_prueba <- weather %>%
filter(temp %in% c(NA))
tabla_prueba
View(tabla_prueba)
weather %>%
filter(temp %in% c(NA)) %>%
summarize(num_rows=n())
weather %>%
filter(origin=="JFK") %>%
summarize(temp_minima=min(temp,na.rm=T), temp_maximo=max(temp,na.rm=T))
weather$origi
weather$origin
View(weather)
unique(weather$origin)
tabla_prueba <- weather %>%
group_by(month)
# Con esto acreamos una tabla por cada mes
View(tabla_prueba)
weather %>%
group_by(month) %>%
summarise(media=mean=temp,na.rm=T)
weather %>%
group_by(month) %>%
summarise(media=mean(temp),na.rm=T)
weather %>%
group_by(month) %>%
summarise(media=mean(temp,na.rm=T))
weather %>%
filter(hour>=8,hour<=20) %>%  # Hasta aqui incluimos desde 8am y 8pm
group_by(day,month) %>%
summarise(temp_media=mean(temp,na.rm=T))
month
month()
months()
ejercicio <- weather %>%
filter(hour>=8,hour<=20) %>%  # Hasta aqui incluimos desde 8am y 8pm
group_by(day,month) %>%
summarise(temp_media=mean(temp,na.rm=T))
